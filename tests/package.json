{
    "name": "bunup-tests",
    "private": true,
    "version": "0.3.74",
    "scripts": {
        "build": "bunup",
        "dev": "bunup --watch"
    },
    "devDependencies": {
        "bunup": "workspace:*"
    }
}
