{
    "name": "bunup-tests",
    "private": true,
    "version": "0.3.86",
    "scripts": {
        "build": "bunup",
        "dev": "bunup --watch"
    },
    "devDependencies": {
        "bunup": "workspace:*"
    }
}
