{
    "name": "bunup-tests",
    "private": true,
    "version": "0.3.88",
    "scripts": {
        "build": "bunup",
        "dev": "bunup --watch"
    },
    "devDependencies": {
        "bunup": "workspace:*"
    }
}
