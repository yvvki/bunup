{
    "name": "bunup-tests",
    "version": "0.3.92",
    "private": true,
    "dependencies": {
        "bunup": "workspace:*"
    },
    "scripts": {
        "build": "bunup fixtures/src/index.mts --dts"
    }
}
