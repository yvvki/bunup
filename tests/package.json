{
    "name": "bunup-tests",
    "private": true,
    "version": "0.3.77",
    "scripts": {
        "build": "bunup",
        "dev": "bunup --watch"
    },
    "devDependencies": {
        "bunup": "workspace:*"
    }
}
