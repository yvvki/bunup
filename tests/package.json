{
    "name": "bunup-tests",
    "version": "0.4.2",
    "private": true,
    "dependencies": {
        "bunup": "workspace:*"
    },
    "scripts": {
        "build": "bunup fixtures/src/index.ts --dts --shims --format esm,cjs --clean"
    }
}
