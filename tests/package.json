{
    "name": "bunup-tests",
    "version": "0.4.0",
    "private": true,
    "dependencies": {
        "bunup": "workspace:*"
    },
    "scripts": {
        "build": "bunup fixtures/src/index.mts --dts"
    }
}
