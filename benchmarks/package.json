{
	"name": "benchmarks",
	"private": true,
	"scripts": {
		"benchmark": "bun run src/main.mjs && node src/tsup.mjs"
	},
	"devDependencies": {
		"tsdown": "^0.9.6",
		"tsup": "^8.4.0",
		"unbuild": "^3.5.0"
	},
	"dependencies": {
		"@types/node": "^20.17.30"
	}
}
