{
    "name": "benchmarks",
    "private": true,
    "version": "0.4.18",
    "scripts": {
        "benchmark": "bun run src/main.mjs && node src/tsup.mjs"
    },
    "devDependencies": {
        "bunup": "workspace:*",
        "tsdown": "^0.6.10",
        "tsup": "^8.4.0",
        "unbuild": "^3.5.0"
    },
    "dependencies": {
        "@types/node": "^20.17.30"
    }
}
